[project]
name = "ci-cd-demo"
version = "0.1.0"
description = "A hands-on GitHub CI/CD demonstration"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
